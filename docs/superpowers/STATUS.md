# Project Status

> Lifted from CLAUDE.md on 2026-05-24 (docs-restructure Phase C). This file is loaded by both Codex (via root AGENTS.md cascade) and Claude Code (via root CLAUDE.md pointer + sibling CLAUDE.md in this folder), so any tool sees current project state.

- **Spec 0 (Storylet Backbone)** — ✅ shipped (`45b38bf`).
- **Spec 1 (Enlisted Home Surface)** — ✅ shipped on `development` (commits `fc93285` → `0390fdf`). Living reference: [docs/Features/Content/home-surface.md](../../docs/Features/Content/home-surface.md).
- **Spec 2 (Orders Surface)** — ✅ shipped on `development` (Phase A + B, ~100 storylets in commits `f8e028b` → `deb88db`). Legacy `src/Features/Orders/` + `ModuleData/Enlisted/Orders/` deleted 2026-04-21 (`a8719bb`). Old plan archived to [docs/superpowers/plans/archive/2026-04-20-orders-surface.md](plans/archive/2026-04-20-orders-surface.md). Remaining Orders tasks migrated into the five-plan integration roadmap below.
- **Five-plan integration roadmap (2026-04-22)** — Plans 1-4 ✅ shipped + smoked on `development`; Plan 5 🟡 code-shipped, in-game smoke pending. Integration spec: [docs/superpowers/specs/2026-04-21-plans-integration-design.md](specs/2026-04-21-plans-integration-design.md). Design spec (truth layer): [docs/superpowers/specs/2026-04-21-enlisted-campaign-intelligence-design.md](specs/2026-04-21-enlisted-campaign-intelligence-design.md). Save-definer offsets claimed: 48 (intelligence snapshot), 49 (signal record), 50 (duty cooldown).
- **Specs 3–5** — Land/Sea, Promotion+Muster, Quartermaster — not yet started; save-definer class offset range 51-70 reserved (offsets 48 + 49 + 50 claimed by Career-loop family Plans 1 + 3 + 4; offsets 54-58 + enum 84 claimed by CK3 wanderer Plan 1).
- **CK3 wanderer 7-plan family** — Plans 1, 2, 3, 6 ✅ shipped on `development`. Plans 4 and 5 🟡 on feature branches, not yet merged. Plan 7 not started. Spec: [docs/superpowers/specs/2026-04-24-ck3-wanderer-systems-analysis.md](specs/2026-04-24-ck3-wanderer-systems-analysis.md). Locked architecture brief: [docs/architecture/ck3-wanderer-architecture-brief.md](../../docs/architecture/ck3-wanderer-architecture-brief.md).
  - **Plan 1** — Architecture Foundation. ✅ shipped. Substrate Plans 2-7 inherit: 5 save-class offsets (54 `PatronRoll` / 55 `PatronEntry` / 56 `ContractActivity` / 57 `EndeavorActivity` / 58 `LifestyleUnlockStore`) + enum 84 `FavorKind`, four CampaignBehaviors (`PatronRollBehavior`, `LifestyleUnlockBehavior`, `RankCeremonyBehavior` on `OnTierChanged`, `PersonalKitTickHandler` hourly+daily), `ModalEventBuilder` helper centralizing the `Storylet → BuildModal → EmitCandidate` pipeline, `CompanionAssignmentManager.IsAssignedToEndeavor` extension, stay-back gate fix at every tier. Plan + verification at [docs/superpowers/plans/2026-04-24-ck3-wanderer-architecture-foundation*.md](plans/).
  - **Plan 2** — Companion Substrate. ✅ shipped + bugfixed (`57941d2` → `4dfe719`). Six wanderer-mechanics companions (Sergeant T1, Field Medic + Pathfinder T3, Veteran T5, QM Officer + Junior Officer T7) spawn via predicate-based troop selection, persist via 24 SyncKey fields on EnlistmentBehavior, fight in lord's battles, can be killed (`OnHeroKilled` slot-clear), released on discharge for per-Lord lifecycle. Six dialog catalogs (~120 nodes, three archetype variants per node) under `ModuleData/Enlisted/Dialogue/companion_*.json` with companion-only loader. Camp menu slot 6 = Talk-to companion (sea variant via `OpenConversationMission`). Follow-up commits fixed QM log pollution (`935afe3`) and dialog token interpolation (`4dfe719`). Living reference: [docs/Features/Companions/companion-archetype-catalog.md](../../docs/Features/Companions/companion-archetype-catalog.md).
  - **Plan 3** — Rank-Ceremony Arc. ✅ shipped (`b222ce5`). Five ceremonies at retained tier transitions (T1→T2, T2→T3, T4→T5, T6→T7 commission, T7→T8); colliding tiers (newTier 4/6/9) deliberately skipped — `PathCrossroadsBehavior` already fires Modal storylets there. 5 ceremony JSON files = 20 storylets × 64 options across base + 3 cultural variants; ~188 inline loc-keys; scripted-effects catalog +44 (10 trait drift + 34 witness reactions × 7 archetypes). Validator Phase 20 (ceremony completeness, fail-closed) added. No new save-definer offsets — choice memory rides on `FlagStore` bool keys (`ceremony_fired_t{N}`, `ceremony_choice_t{N}_<choice_id>`).
  - **Plan 4** — Officer Trajectory. 🟡 on `feature/plan4-officer-trajectory` (HEAD `4bd31eb`), Phase 4 complete, not yet merged to `development`.
  - **Plan 5** — Endeavor System. 🟡 on `feature/plan5-endeavor-system` (HEAD `90db689`), Phase F complete, not yet merged to `development`.
  - **Plan 6** — Roll of Patrons. ✅ shipped to `development` (`4c6ef61` Phase A → `f498477` Phases B+C → `adb61e8` Phase D verification → `dd6cc45` token-interpolation fix). Substrate population (T1-T6), wiring/dialogs/favor storylets (T7-T15), verification report (T16-T18). Plan + verification at [docs/superpowers/plans/2026-04-24-ck3-wanderer-roll-of-patrons*.md](plans/). Living reference: [docs/Features/Patrons/patron-favor-catalog.md](../../docs/Features/Patrons/patron-favor-catalog.md).
  - **Plan 7** — not started.
- **Legacy decisions / orchestrator / Camp* retirement (2026-04-25, `aa3ef16`)** — deleted `DecisionCatalog`, `DecisionManager`, `ContentOrchestrator`, `OrchestratorOverride`, `CampOpportunityGenerator`, `CampScheduleManager`, `CampRoutineProcessor`, 8 Camp/Models POCOs, `ModuleData/Enlisted/Decisions/*.json`, `Config/orchestrator_overrides.json`. Re-homed: illness-onset modal → `CompanySimulationBehavior.CheckIllnessOnset` (Phase 7 daily tick); quiet-day predicate → `GlobalEventPacer.ComputeIsQuietDay` on day rollover. `EnlistedMenuBehavior` lost ~830 lines of decision-handling + the decisions accordion + 5 decision slots. Save-definer offsets 118 (`OpportunityType`) and 119 (`CampMood`) freed.
- **Docs + AGENTS.md restructure (2026-05-24)** — Phases A + B complete. Phase C in progress (this file is the Phase C output). See [plans/2026-05-24-docs-and-agents-md-restructure.md](plans/2026-05-24-docs-and-agents-md-restructure.md).
